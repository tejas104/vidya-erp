{
  "name": "@vidya/module-portal",
  "version": "0.1.0",
  "private": true,
  "description": "Vidya student portal module (no tables): self-scoped read-only views for signed-in students, resolved through the people identity link — never a studentId parameter. Composes the public PeopleDirectory + AcademicsReadModel; W1 of the SaaS program.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vidya/module-academics": "workspace:*",
    "@vidya/module-people": "workspace:*",
    "@vidya/platform": "workspace:*",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "pino": "^9.7.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.0"
  }
}
