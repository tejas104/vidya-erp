{
  "name": "@vidya/module-fees",
  "version": "0.1.0",
  "private": true,
  "description": "Vidya fees module (fee_): heads, per-class structures, invoices (generated in bulk by a worker job), payments with a monotonic receipt sequence, and adjustments (scholarship/fine/refund/waiver). Integer paise everywhere. Accountant self-service via /manage/collections; student self-scope via my-fees.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vidya/module-people": "workspace:*",
    "@vidya/platform": "workspace:*",
    "drizzle-orm": "^0.44.2",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "pino": "^9.7.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.0"
  }
}
